[project]
name = "reserv-system"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
    "fastapi[standar,standard]>=0.139.2",
    "pwdlib[argon2]>=0.3.0",
    "pydantic>=2.13.4",
    "pyjwt>=2.13.0",
]

[tool.fastapi]
entrypoint = "app.main:app"
